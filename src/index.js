import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';

const element = (
  <div>
    Hola mundo!
  </div>
);

ReactDOM.render(
  element,
  document.getElementById('root')
);

